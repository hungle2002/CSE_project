import {default as conditionRoute} from './conditions';
import {default as deviceRoute} from './devices';
import {default as settingsRoute} from './settings';
import {default as authRoute} from './auth';

const route = {
  conditionRoute,
  deviceRoute,
  settingsRoute,
  authRoute,
};

export default route;
